

const Spinner = () => {
    return (
        <div className='min-h-screen flex items-center justify-center'>
            <button className="custom-btn loading">loading</button>
        </div>
    );
};

export default Spinner;